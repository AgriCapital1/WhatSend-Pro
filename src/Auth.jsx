import React,{useState} from 'react';
import { ArrowRight, Loader2, Eye, EyeOff } from 'lucide-react';
import { supabase } from './supabase.js';
export default function Auth({onAuth}){const[mode,setMode]=useState('login'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[show,setShow]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState(''),[message,setMessage]=useState('');
async function submit(e){e.preventDefault();setError('');setMessage('');setLoading(true);if(!supabase){setError('Supabase n’est pas configuré dans .env.');setLoading(false);return}const r=mode==='login'?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password});setLoading(false);if(r.error){setError(r.error.message);return}if(mode==='signup'){setMessage('Compte créé. Vérifiez votre e-mail si la confirmation est activée.')}else onAuth(r.data.user)}
return <div className="authPage"><div className="authCard"><div className="authLogo"><div className="brandMark">➤</div><div><strong>WhatSend <span>Pro</span></strong><small>WhatsApp professionnel, simplement.</small></div></div><div className="authIntro"><div className="eyebrow">ESPACE PROFESSIONNEL</div><h1>{mode==='login'?'Bienvenue sur WhatSend Pro':'Créer votre espace'}</h1><p>{mode==='login'?'Gérez vos communications WhatsApp Business depuis un seul espace.':'Créez votre compte et commencez votre configuration.'}</p></div><form onSubmit={submit}><label>E-mail<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required placeholder="vous@entreprise.com"/></label><label>Mot de passe<div className="password"><input value={password} onChange={e=>setPassword(e.target.value)} type={show?'text':'password'} required minLength="6" placeholder="••••••••"/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{error&&<div className="formError">{error}</div>}{message&&<div className="formSuccess">{message}</div>}<button className="primary full" disabled={loading}>{loading?<Loader2 className="spin" size={17}/>:<ArrowRight size={17}/>} {mode==='login'?'Se connecter':'Créer mon compte'}</button></form><button className="switchAuth" onClick={()=>{setMode(mode==='login'?'signup':'login');setError('');setMessage('')}}>{mode==='login'?'Pas encore de compte ? Créer un espace':'Déjà un compte ? Se connecter'}</button><small className="legal">Conditions d’utilisation · Politique de confidentialité</small></div></div>}