import "jsr:@supabase/functions-js/edge-runtime.d.ts";
const json=(x,status=200)=>new Response(JSON.stringify(x),{status,headers:{"Content-Type":"application/json"}});
Deno.serve(async(req)=>{try{if(req.method==="GET"){const u=new URL(req.url);const mode=u.searchParams.get("hub.mode");const token=u.searchParams.get("hub.verify_token");const challenge=u.searchParams.get("hub.challenge");if(mode==="subscribe"&&token&&token===Deno.env.get("META_VERIFY_TOKEN"))return new Response(challenge||"",{status:200});return new Response("Forbidden",{status:403});}if(req.method==="POST"){const body=await req.json();console.log("WhatsApp webhook event",JSON.stringify(body));return json({received:true});}return new Response("Method Not Allowed",{status:405});}catch(e){return json({error:String(e?.message||e)},500)}});