import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, content-type","Content-Type":"application/json"};
Deno.serve(async(req)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors});try{const auth=req.headers.get("Authorization")||"";const token=auth.replace("Bearer ","");if(!token)return new Response(JSON.stringify({error:"Unauthorized"}),{status:401,headers:cors});const sb=createClient(Deno.env.get("SUPABASE_URL")!,Deno.env.get("SUPABASE_ANON_KEY")!,{global:{headers:{Authorization:auth}}});const {data:{user},error:ue}=await sb.auth.getUser(token);if(ue||!user)return new Response(JSON.stringify({error:"Unauthorized"}),{status:401,headers:cors});const {account_id,to,message,template_name,template_language}=await req.json();if(!account_id||!to)return new Response(JSON.stringify({error:"account_id and to are required"}),{status:400,headers:cors});const {data:account,error:ae}=await sb.from("whatsapp_accounts").select("id,user_id,phone_number_id,status").eq("id",account_id).eq("user_id",user.id).single();if(ae||!account)return new Response(JSON.stringify({error:"WhatsApp account not found"}),{status:404,headers:cors});const accessToken=Deno.env.get("META_ACCESS_TOKEN");if(!accessToken)return new Response(JSON.stringify({error:"META_ACCESS_TOKEN is not configured"}),{status:503,headers:cors});const version=Deno.env.get("META_GRAPH_API_VERSION")||"v23.0";const payload=template_name?{messaging_product:"whatsapp",to,type:"template",template:{name:template_name,language:{code:template_language||"fr"}}}:{messaging_product:"whatsapp",to,type:"text",text:{body:message||""}};const response=await fetch(`https://graph.facebook.com/${version}/${account.phone_number_id}/messages`,{method:"POST",headers:{"Authorization":`Bearer ${accessToken}`,"Content-Type":"application/json"},body:JSON.stringify(payload)});const result=await response.json();if(!response.ok)return new Response(JSON.stringify({error:"Meta API error",details:result}),{status:response.status,headers:cors});await sb.from("messages").insert({user_id:user.id,whatsapp_account_id:account.id,direction:"outbound",status:"sent",message_type:template_name?"template":"text",body:message||template_name,meta_message_id:result?.messages?.[0]?.id||null});return new Response(JSON.stringify({success:true,data:result}),{headers:cors});}catch(e){return new Response(JSON.stringify({error:String(e?.message||e)}),{status:500,headers:cors});}});